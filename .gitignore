# Secrets must never be committed (spec: no secrets in repo).
.env
*.env
*.key
*.pem
secrets.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.hypothesis/
.venv/
venv/
sbom.json

# CI / runtime artifacts (regenerated by the battery; never committed)
reports/
*.pid
